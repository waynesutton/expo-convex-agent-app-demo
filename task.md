# Tasks

## To Do

- Wire Convex Auth (sign in with Apple first). See docs/auth.md.
- Swap chunked writes for the Persistent Text Streaming component when message volume justifies it.
- EAS Workflows CI: convex deploy and eas update in one pipeline.
- Mark the GitHub repo as a template: `gh repo edit waynesutton/expo-convex-agent-app-demo --template`.

## Recently Completed

- 2026-08-19 18:27 UTC - README rewritten as the template source of truth: what it is, features, folder structure, static hosting paths, copyable keyless setup prompt, Convex API table, components, GitHub plus Convex deploy. Landing copy prompt, docs/one-prompt-setup.md, and AGENTS.md now match: zero API keys, CONVEX_AGENT_MODE=anonymous first, FIRECRAWL_API_KEY unset required. REPO_URL set to https://github.com/waynesutton/expo-convex-agent-app-demo. Landing hero spacing tightened and republished.
- 2026-08-19 18:12 UTC - Landing hero whitespace reduced (nav, header, CTAs, sections, footer) and landing uploaded to prod at https://sincere-marmot-167.convex.site.
- 2026-08-17 07:35 UTC - Mobile sidebar menu on the landing site. Both landing pages hide the nav link row below 720px behind a menu button that opens a right side drawer: greyred tokens, scrim, body scroll lock, closes on scrim, close button, link tap, or Escape, reduced motion respected. The docs page drawer also carries the full on this page section list. Verified live on prod at a phone viewport after redeploy.
- 2026-08-17 07:20 UTC - First production site deploy plus the missing env var fix. site:deploy failed against a fresh prod deployment because the Firecrawl component requires FIRECRAWL_API_KEY at push and prod had no env vars; set the unset placeholder and DEMO_MODE=true on prod, deployed the backend, uploaded both sites. Landing setup prompt gains optional step 8 (prod env vars then npm run site:deploy), docs.html host section and docs/static-hosting.md document the first deploy prerequisite. Live at https://sincere-marmot-167.convex.site with /, /docs.html, and /app/ all returning 200.
- 2026-08-17 06:58 UTC - App name read-only in demo mode. Settings shows the wordmark as text with a fork note instead of the rename input, and users.setAppName throws a ConvexError when DEMO_MODE=true. Verified live: card renders read-only expo-demo. Docs: docs/demo-mode.md, changelog.
- 2026-08-17 06:55 UTC - Text only public demo and no input focus rings. Demo mode hides the composer photo attach button (new Composer allowAttachments prop) and the bot avatar upload links, with server enforcement in files.generateUploadUrl, messages.send (attachment reject), and bots.setAvatar (new image reject, clear allowed). New noFocusRing style in lib/theme.ts removes the web focus outline on every TextInput. Verified: both workspaces typecheck, backend pushed, site republished, browser check confirmed the attach button is gone and the focused composer computes outline none. Docs: docs/demo-mode.md, changelog.
- 2026-08-17 06:45 UTC - Firecrawl moved to the official @firecrawl/firecrawl-convex component. Mounted in convex.config.ts with typed env (FIRECRAWL_API_KEY required at push, placeholder "unset" documented for keyless installs; webhook at /firecrawl/ ready for durable crawls); new convex/firecrawl.ts (client, placeholder aware configured helper, status query); firecrawl_scrape, firecrawl_search, and skill import URL reads go through the component; Settings gains a Firecrawl status card. Docs synced: docs/integrations.md, README (env block, components list, deploy note), AGENTS.md setup steps, landing index (setup prompt, complete components list) and docs.html (tool table, settings badge note), files.md, changelog. Skipped the attached WorkOS auth skill per AGENTS.md hard rule 1 (Convex Auth only). Verified: convex dev mounts clean, status query returns configured false on placeholder, webhook route answers at /firecrawl/webhook with landing unaffected, both workspaces typecheck, site republished. PRD: prds/firecrawl-component.md.
- 2026-08-17 06:15 UTC - Demo mode hardening. DEMO_MODE env var flips the app into a public demo posture: Settings drops all key entry for a read only provider status card (Active label, zero key material), server side moderation (profanity everywhere; links, 2,000 char cap, and rate limits in demo mode via @convex-dev/rate-limiter), agent.runAgent gains an optional apiKey with a server side DEMO_LLM_* fallback, public banners on home and chat screens, the 5 minute reset cron also clears idle visitor content, About screen links the landing page via landingUrl(), landing copy updated. Docs: new docs/demo-mode.md with fork and auth instructions, README, files.md. Verified: both workspaces typecheck, convex dev deployed the rate limiter component and new functions clean. PRD: prds/demo-mode-hardening.md.
- 2026-08-17 05:40 UTC - One URL hosting on Convex. Installed @convex-dev/static-hosting with two instances in convex.config.ts (landing at /, demoApp at /app/) and moved app HTTP routes under /api, so the AgentMail webhook is now /api/agentmail/webhook. Expo web export wired for the sub path (app.json experiments.baseUrl "/app", build:web script with VITE_CONVEX_URL passthrough). New site:preview and site:deploy scripts at the root and in packages/backend. Landing pages: DEMO_URL defaults to /app/, new run it on your phone section with a QR rendered locally by vendored qrcode.min.js, docs.html gains a host it on Convex section. Docs synced: new docs/static-hosting.md, README (landing section, components list, docs list), docs/deploy.md, docs/integrations.md webhook URL, files.md. Verified: components installed on dev deployment, both workspaces typecheck, site:preview uploaded both sites, curl checks (/, /docs.html, /app/, 404 on unknown paths, webhook routes at /api and 404s at the old path), browser check confirmed QR renders and the /app/ demo boots with the seeded roster and chats. PRD: prds/convex-site-hosting.md.
- 2026-08-17 03:55 UTC - App docs page on the landing site. New landing/docs.html in the same greyred style: a 12 section newbie walkthrough compiled from README and docs/ (what this is, prerequisites, template vs fork, convex dev explained step by step, anonymous agent mode, EAS integration path, env file and expo start, BYOK provider table and key lifecycle, workspace tool env vars, agent loop, demo reset, mentions, skills, memory, reminders, search, avatars, rename checklist, deploy, troubleshooting table, repo rules). index.html nav and footer gain an App docs link and the hero "Read the docs" button now opens the page. Verified in browser: nav, TOC anchors, code blocks, tables, hidden demo/repo links. PRD: prds/landing-app-docs-page.md.
- 2026-08-17 03:40 UTC - App rename for forks, components rationale, Expo guide link. users.appName field with setAppName (trim, 30 cap, empty clears) and users.get; Settings gains an App name card; home header and sidebar wordmark render the custom name reactively. README gains a "Why not AI Agent, Workflow, RAG" section with upgrade paths; landing page links the Expo Convex guide (eas integrations:convex:connect) and the same upgrade paths. Verified: rename round trip in browser (set, reload seed, clear back to default confirmed in db), convex deploys clean, both workspaces typecheck. PRD: prds/app-rename-components-expo-link.md.
- 2026-08-17 03:30 UTC - Landing page demo app links. New DEMO_URL constant next to REPO_URL: unhides an "Open the demo app" hero button plus Demo links in the nav and footer, and turns the three screenshots into links to the running app. Fixed the pre-existing bug where the hidden Fork it button rendered anyway because .btn's display overrode the hidden attribute. Docs synced: README landing section, files, changelog. Verified in browser: default state shows no demo links, simulated DEMO_URL shows all of them with pointer cursor on the shots.
- 2026-08-17 02:22 UTC - Landing page: prompt pile replaced with a compact copy card (hidden pre keeps both copy buttons working) and three mobile screenshots added (home, demo chat, settings) captured from the live web app at 375x640, 2x scale, stored in landing/screens/ with aspect-ratio sizing and lazy loading. Verified in browser at desktop and 390px widths; copy source confirmed intact. PRD: prds/landing-screenshots-prompt.md.
- 2026-08-17 02:10 UTC - AgentMail moved to the official @agentmail/convex component. Mounted in convex.config.ts (installs agentmail plus its send and callback workpools); agentmail_send enqueues via the new email.sendFromTool internal mutation for durable delivery with retries; agentmail_list reads threads through the component client; inbound webhook mounted at /agentmail/webhook in new convex/http.ts. Settings screen gains an AgentMail card (configured state, inbox email, setup commands). Docs synced: docs/integrations.md, README (env table, setup block, components list), landing page components list, files, changelog. Verified: component installed on dev deployment, functions ready, both workspaces typecheck, settings card confirmed in browser. PRD: prds/agentmail-component.md.
- 2026-08-17 01:57 UTC - PNG bot avatars, full text search, Beautiful UI updates. bots.avatarId plus setAvatar with double validation (PNG, 2 MB cap, storage cleanup on reject/replace/delete); avatarUrl resolved in bots, threads, messages, and search queries; BotAvatar renders images; upload UI in bot settings with expo-image-picker. Search: three schema search indexes, convex/search.ts search.all, app/search.tsx screen, magnifying glass in home header and sidebar. Composer attachment now previews as a thumbnail with remove. Docs synced: README, docs/file-uploads.md, changelog, files. Verified: convex dev deploys clean, both workspaces typecheck, web smoke test of search and avatar plumbing. PRD: prds/avatars-search-ui.md.
- 2026-08-17 01:55 UTC - Grey led palette and quieter type. App ground moved to soft grey #FAFAFA with palette aligned tokens (ink #171717, slate #4D4D4D, faint #888888, signal #EE0000); bot identity colors rebuilt without purple (ink, graphite, slate, blue, teal, amber, coral) with botInitialColor for dark initials on light colors; wordmark weight 400 and bot names/titles 500 across roster, sidebar, chat, pickers; landing page tokens and heading weights synced; demo seeds recolored and existing dev deployment bots patched via bots.update. Docs synced: docs/design.md, PRD.md, changelog. Verified: native app typechecks clean.
- 2026-08-17 01:10 UTC - Posed demo, reminders, skills, memory, groups, bot settings, landing page. Seeded demo workspace (Chief of Staff, Coach, Research Bot plus a group thread) reset every 5 minutes by cron with an in chat DemoBanner and demoSeededAt backfill; per bot reminders on a 5 minute cron with a global toggle in settings; skill_import and remember tools with a skills table; group chat creation screen; bot settings screen with delete and cancel on create; ThinkingDots streaming indicator; greyred landing page in landing/index.html. Docs synced: README, docs/design.md, changelog, files. Verified: convex dev deploys clean, both workspaces typecheck, web smoke test shows seeded demo and banner. PRD: prds/demo-skills-reminders-landing.md.
- 2026-08-17 00:40 UTC - @mentions for bots. Type @ in the composer for an autocomplete strip of your bots; a message that mentions bots routes the run to those bots only, in mention order, and mentioning a bot outside the thread adds it first. New lib/mentions.ts (parse, active query, insert), Composer suggestion strip, chat send routing through the existing runAgent botIds arg, semibold @tokens in user bubbles, persona prompt now explains @Name. Verified: parser edge cases in node (emails, prefix names, multi word names, dedupe), both workspaces typecheck, autocomplete confirmed in browser. PRD: prds/at-mentions.md.
- 2026-08-16 23:50 UTC - Concentrate gateway BYOK, Merge and Runware tools, About screen. Concentrate (concentrate.ai) added as the fifth on device provider through the OpenAI compatible adapter; merge_list tool over the Merge unified API (MERGE_API_KEY + MERGE_ACCOUNT_TOKEN) and runware_generate_image tool (RUNWARE_API_KEY) registered for every bot; About modal in settings links README, docs, Convex, and Expo through lib/links.ts REPO_URL. Docs updated: README, docs/byok.md, docs/integrations.md. Verified: convex dev deploys clean, both workspaces typecheck, five provider chips and About modal confirmed in browser. PRD: prds/gateway-tools-and-about.md.
- 2026-08-16 23:35 UTC - Grok Bot style multi bot UI. Bots table with name, identity color, and job; bot roster home screen; create bot modal with color swatches and live avatar preview; chat restyled with ink user bubbles and mist bot bubbles; teammate picker adds bots to a thread and the agent loop runs one persona pass per bot so they read and build on each other's replies; desktop sidebar at 900px and up on Expo web; light mode default with Phosphor icons. Verified in browser: roster, create flow, group thread title, sidebar. PRD: prds/grok-style-bots-ui.md.
- 2026-08-16 23:00 UTC - Full template setup and verification in Convex anonymous agent mode. Backend deployed to a local anonymous deployment (schema, indexes, pushNotifications component, codegen), both workspaces typecheck clean, Expo web demo verified: thread list, thread creation, chat composer, BYOK missing key guard, settings screen. PRD: prds/expo-demo-PRD.md.
- 2026-08-16 23:00 UTC - Web preview support: lib/keys.ts falls back to localStorage on web (secure-store is native only), lib/push.ts skips push registration and notification listeners on web.
- 2026-08-16 23:00 UTC - Composer UX fix: draft text is kept when a send is blocked by the missing key guard instead of being cleared.
- 2026-08-16 23:00 UTC - Version alignment via expo install --fix: react-native 0.79.6, react-native-safe-area-context 5.4.0. Added react-native-web and react-dom for web preview.
- 2026-08-16 23:00 UTC - Docs: added docs/github.md (init, push, template repo settings), agent mode and web preview sections in docs/setup.md, unattended agent note in docs/one-prompt-setup.md, README status and links updated.
